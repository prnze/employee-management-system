FileX — Required Packages
=========================

Runtime (already in package.json):

  @ffmpeg/ffmpeg              ^0.12.15   # WebAssembly audio/video conversion
  @ffmpeg/util                ^0.12.2    # ffmpeg.wasm helpers (toBlobURL)
  heic2any                    ^0.0.4     # HEIC/HEIF decoder
  browser-image-compression   ^2.0.2     # Quality-targeted image re-encoding
  pdf-lib                     ^1.17.1    # PDF create / merge / split / rotate
  pdfjs-dist                  ^4.7.76    # PDF text + page raster extraction
  jszip                       ^3.10.1    # ZIP create / extract, TAR repack

  framer-motion               ^12.x      # Spring animations, micro-interactions
  zustand                     ^5.x       # State store (jobs, history, theme)
  react-hook-form             ^7.x       # Forms (settings, presets)
  zod                         ^4.x       # Validation
  sonner                      ^2.x       # Toasts
  cmdk                        ^1.x       # Command palette
  react-dropzone              ^15.x      # File drag & drop
  lucide-react                latest     # Icon set
  @fontsource/ibm-plex-mono   ^5.x       # Self-hosted IBM Plex Mono
  @tanstack/react-query       ^5.x       # Data layer (optional)

Already provided by the template:
  react, react-dom, @tanstack/react-router, @tanstack/react-start,
  tailwindcss v4, vite, typescript

No native binaries, no Docker, no Redis required for v1.
All conversion runs in-browser via WebAssembly.

Optional FileX Pro backend (NOT included in this build):
  - ffmpeg (native)
  - imagemagick / graphicsmagick
  - libheif
  - libreoffice (headless)
  - ghostscript
  - poppler-utils
  - pandoc
